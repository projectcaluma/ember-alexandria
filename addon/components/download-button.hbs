<UkButton
  class="uk-width-1-1"
  @size="large"
  @color="primary"
  @loading={{this.download.isRunning}}
  {{on "click" (perform this.download)}}
>
  {{t "alexandria.document-download.button" numDocs=@documents.length}}
</UkButton>