<UkButton
  class="uk-width-1-1"
  @size="large"
  @color="primary"
  @loading={{this.documents.download.isRunning}}
  data-test-download-button
  {{on "click" (fn this.documents.download.perform @documents)}}
>
  {{t "alexandria.document-download.button" numDocs=@documents.length}}
</UkButton>