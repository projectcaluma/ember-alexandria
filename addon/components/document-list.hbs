<table
  class="uk-table uk-table-divider uk-table-striped uk-table-hover document-list"
>
  <caption></caption>
  <thead>
    <tr>
      <th class="uk-table-shrink">
        Icon
      </th>
      <th class="uk-table-expand">
        Title
        <FaIcon
          @icon="sort"
          @prefix="fas"
          @size="1x"
          @class="uk-margin-small-left"
          role="button"
          data-test-sort-title
          {{on "click" (fn @setSort "title")}}
        />
      </th>
      <th>
        Created
        <FaIcon
          @icon="sort"
          @prefix="fas"
          @size="1x"
          @class="uk-margin-small-left"
          data-test-sort-title
          role="button"
          {{on "click" (fn @setSort "createdAt")}}
        />
      </th>
      <th>
        Group
        <FaIcon
          @icon="sort"
          @prefix="fas"
          @size="1x"
          @class="uk-margin-small-left"
          data-test-sort-title
          role="button"
          {{on "click" (fn @setSort "createdByGroup")}}
        />
      </th>
    </tr>
  </thead>
  <tbody>
    {{#each @fetchedDocuments as |document|}}
      <DocumentListItem
        @document={{document}}
        @isSelected={{includes document.id (map-by "id" @selectedDocuments)}}
        @onClickDocument={{@onClickDocument}}
      />
    {{/each}}
  </tbody>
</table>